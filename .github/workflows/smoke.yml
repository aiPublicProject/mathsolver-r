name: smoke (real API)
on:
  workflow_dispatch: {}
jobs:
  smoke:
    runs-on: ubuntu-latest
    env:
      SMOKE_API_KEY: ${{ secrets.SMOKE_API_KEY }}
      SMOKE_BASE_URL: ${{ secrets.SMOKE_BASE_URL }}
      SMOKE_MODEL: ${{ secrets.SMOKE_MODEL }}
    steps:
      - uses: actions/checkout@v4
      - name: Require secret
        run: |
          if [ -z "$SMOKE_API_KEY" ]; then
            echo "::error::SMOKE_API_KEY secret not configured (repo/org Settings - Secrets - Actions)"
            exit 1
          fi
      - uses: r-lib/actions/setup-r@v2
      - run: Rscript -e "r <- Sys.getenv(\"RSPM\"); if (!nzchar(r)) r <- \"https://cloud.r-project.org\"; install.packages(c(\"jsonlite\", \"httr2\"), repos=r)"
      - run: Rscript tests/run.R
